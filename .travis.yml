dist: trusty
sudo: required
language: node_js
node_js:
  - "8"
addons:
  chrome: stable
before_install:
  - npm install
  - npm install -g qunit
  - npm install -g eslint

script: npm test && eslint js/
